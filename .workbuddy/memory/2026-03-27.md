# 2026-03-27 工作日志

## 日线图 MA260 + 偏差值改造

修改 `chart_generator.py`：
1. `MAX_BARS["日线"]`: 120 → 300，确保 MA260 计算有足够历史数据
2. `MA_COLORS` 新增 `260: "#aaaaff"`（淡蓝色）
3. `PERIOD_MAS["日线"]`: [5,10,20,60,120,250] → [260]，只保留年线
4. 均线绘制后，日线专用逻辑：取最后一根收盘价与 MA260 末值计算偏差值 = 收盘/MA260 - 1，以百分比显示在 K 线区左上角（正偏差红色 / 负偏差绿色，格式如 `偏差: +5.3%`）

测试：000001 平安银行生成成功，偏差值正确显示。

## 日线左上角显示格式调整（2026-03-27 19:54）

原来 MA260 图例标签和偏差值 text 重叠，改为：
- 去掉日线 legend，改用两段 text 紧排一行
- 格式：`MA260=11.4`（淡蓝色）+ `  偏差=-2.7%`（涨红跌绿）
- 图例逻辑加了 `period != "日线"` 判断，只有其他周期才用 legend

## HTML 报告卡片标题栏添加 MA260 偏差值（2026-03-27 20:18）

修改 `report_generator.py`：
- 新增 `_calc_ma260_deviation(stock_data)` 函数，从日线 DataFrame 计算 MA260 偏差值
- 在每张卡片 `card-header` 的股票名称（label）后面插入 `<span class="ma260-dev">` 显示偏差值
- 格式：`000001 · 平安银行  -1.8%`（涨红跌绿）
- 新增 `.ma260-dev` CSS 样式（font-weight:700, margin-left:10px）
- 顶部导入增加 `import pandas as pd`
